import 'package:flutter/material.dart';

class AndroColors {
  // ── DARK THEME (matches Figma) ────────────────────────────────────────────
  static const darkBackground  = Color(0xFF0B0B1F);   // deep dark navy
  static const darkSurface     = Color(0xFF16162A);   // card background
  static const darkSurface2    = Color(0xFF1E1E35);   // elevated card
  static const darkAccent      = Color(0xFF4A7CF7);   // blue CTA / active
  static const darkAmber       = Color(0xFFF5A524);   // featured / RSVP / active nav
  static const darkText        = Color(0xFFFFFFFF);
  static const darkSecondary   = Color(0xFF8A8AA8);
  static const darkRed         = Color(0xFFEF4444);   // deadline / closes
  static const darkGreen       = Color(0xFF22C55E);   // Workshop green
  static const darkNavBg       = Color(0xFF12122A);   // bottom nav background
  static const darkDivider     = Color(0xFF2A2A40);

  // ── LIGHT THEME ───────────────────────────────────────────────────────────
  static const lightBackground = Color(0xFFF0F0FA);
  static const lightSurface    = Color(0xFFFFFFFF);
  static const lightSurface2   = Color(0xFFF5F5FF);
  static const lightAccent     = Color(0xFF4A7CF7);
  static const lightAmber      = Color(0xFFF5A524);
  static const lightText       = Color(0xFF0B0B1F);
  static const lightSecondary  = Color(0xFF6B6B8A);
  static const lightRed        = Color(0xFFEF4444);
  static const lightGreen      = Color(0xFF22C55E);
  static const lightNavBg      = Color(0xFFFFFFFF);
  static const lightDivider    = Color(0xFFE8E8F0);
}
